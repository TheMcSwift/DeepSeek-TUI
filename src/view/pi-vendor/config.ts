/** Stub for pi's config helpers (the DSH surface has no pi config dirs). */
export function getBinDir(): string {
  return ''
}
export function getThemesDir(): string {
  return ''
}
export function getCustomThemesDir(): string {
  return ''
}
